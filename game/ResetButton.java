package game;

import javax.swing.JButton;

//ResetButton is a custom button for resetting the game.
//Along with Card, this class extends JButton, fulfilling the requirement for a parent class (JButton) with at least two child classes.
public class ResetButton extends JButton {

    public ResetButton(String text) {
        super(text);
    }
// This is a simple demonstration for the inheritance requirement.
}
